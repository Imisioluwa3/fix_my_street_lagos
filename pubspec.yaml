name: fix_my_street_lagos
description: A comprehensive Flutter app with Firebase authentication and real-time data storage
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  supabase_flutter: ^1.10.25
  image_picker: ^0.8.7+5
  geolocator: ^9.0.2
  flutter_dotenv: ^5.1.0
  cupertino_icons: ^1.0.2


  # firebase_core: ^2.24.0
  # firebase_auth: ^4.18.0
  # cloud_firestore: ^4.15.0
  # firebase_storage: ^11.7.0
  # image_picker: ^1.0.4
  # geolocator: ^10.0.0
  # permission_handler: ^11.1.0

dependency_overrides:
  firebase_core_web: ^2.4.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
  assets:
    - .env
    - assets/images/